<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1><%= title %></h1>
  
  <p>The seconds in current time is <%= seconds %></p>
  <% if (seconds % 2 == 0) { %>
    <ul>
      <% items.forEach(element => { %>
       <li><%= element %></li>
      <% }) %>
    </ul>
  <% } else {%>
    <p>Nothing to show here</p>
  <% } %>
  
  <p><%- htmlContent %></p>
  <%-include('footer.ejs') %>
</body>
</html>